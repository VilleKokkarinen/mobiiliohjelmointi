<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.login">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Login">
        <activity
            android:name="com.example.login.ui.home.MainActivity"
            android:exported="true" />
        <activity
            android:name="com.example.login.ui.login.UpdatePassword"
            android:exported="true" />
        <activity
            android:name="com.example.login.ui.login.ForgotPasswordActivity"
            android:exported="true" />
        <activity
            android:name="com.example.login.ui.login.SignupActivity"
            android:exported="true" />
        <activity
            android:name="com.example.login.ui.login.LoginActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>